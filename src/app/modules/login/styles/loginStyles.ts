import {Theme} from "@material-ui/core";

export const loginStyles = (theme: Theme) => ({
    form: {
        margin: "0",
        padding: "2rem"
    },
    submitButton: {
        margin: "1.5rem 0"
    },
})