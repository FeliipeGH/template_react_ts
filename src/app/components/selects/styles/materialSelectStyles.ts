import {Theme} from "@material-ui/core";
import customSelectStyle from "../../../../assets/styles/selectStyles";

export const materialSelectStyles = (theme: Theme) => ({
    ...customSelectStyle,
});