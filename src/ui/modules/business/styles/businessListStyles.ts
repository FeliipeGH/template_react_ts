import {Theme} from "@material-ui/core";
import {button} from "../../../../assets/styles/tss/globalStyles";

export const businessListStyles = (theme: Theme) => ({
    submitButton: {
        ...button,
        margin: "0.5rem 0"
    }
});