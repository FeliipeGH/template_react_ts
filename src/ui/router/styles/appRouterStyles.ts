import {Theme} from "@material-ui/core";
import {circularProgressContent} from "../../../assets/styles/tss/globalStyles";

export const appRouterStyles = (theme: Theme) => ({
    circularProgressContent,
})